# Local build tooling — kept on disk, never deployed to the live site
*.py
_ogfont/
_fontsrc/

# Secrets — kept on disk + deployed in the zip, never in the public repo
api/config.php
phptest.php

# Packaged uploads
*.zip

# OS / editor cruft
Thumbs.db
.DS_Store
desktop.ini
*.log
